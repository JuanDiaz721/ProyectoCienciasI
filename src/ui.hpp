#pragma once

/**
 * @file ui.hpp
 * @brief Declaración de la clase UI (User Interface).
 * * Esta clase maneja el bucle principal de la aplicación,
 * menús y la interacción con el usuario.
 */
class UI {
public:
    /**
     * @brief Inicia y ejecuta el bucle principal de la aplicación.
     */
    void ejecutar();
};